from pydub import AudioSegment
import os

clipdurationMs
fCounter


###
valid filepath checking here
###:


    for au_file in files: 
        sound = au_file.open_as_mp3(au_file)
        for clip in sound[::clipDurationMs]:
            clip.export({basepath} + {u_file} + str(fCounter), format="mp3"
            fCounter+=1
